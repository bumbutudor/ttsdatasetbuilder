# TTS/STT Dataset Builder - Environment Configuration
# Rename this file to .env and adjust values

# ============== Security ==============
# Generate a strong secret key for production
# You can use: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-change-me-in-production

# ============== Database ==============
# SQLite (default, file-based)
DATABASE_URL=sqlite:///./data/app.db

# PostgreSQL (uncomment for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/tts_stt_db

# ============== AI Provider ==============
# Options: "openai" or "ollama"
AI_PROVIDER=openai

# ============== OpenAI Configuration ==============
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL_NAME=gpt-4o-mini

# ============== Ollama Configuration ==============
# For local Vision AI processing (requires Ollama installed)
OLLAMA_MODEL_NAME=gemma3:4b
# If running Ollama in Docker or on another host:
# OLLAMA_HOST=http://ollama:11434

# ============== Whisper Configuration ==============
# HuggingFace model for Romanian speech recognition
# Options:
#   - /models/FineTunedWhisperModel (recommended for server deployments with bind mount)
#   - iRaduS/whisper-romanian-finetune
#   - TransferRapid/whisper-large-v3-turbo_ro
#   - gigant/whisper-medium-romanian
#   - readerbench/whisper-ro
WHISPER_MODEL_NAME=/models/FineTunedWhisperModel
WHISPER_PRELOAD_ON_STARTUP=true
# Optional: required only for private Hugging Face repos
HF_TOKEN=hf_your_token_here

